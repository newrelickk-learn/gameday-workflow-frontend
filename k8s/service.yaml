apiVersion: v1
kind: Service
metadata:
  name: gameday-workflow-frontend
  namespace: gameday-workflow
  labels:
    app: gameday-workflow-frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: gameday-workflow-frontend
